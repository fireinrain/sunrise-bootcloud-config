spring:
  profiles:
    active:
      - dev
---
spring:
  profiles: dev # 开发环境
  application:
    name: microservice-cloud-config-dev

---
spring:
  profiles: test # 开发环境
  application:
    name: microservice-cloud-config-test